# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
dist/
build/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
.claude/
CLAUDE.md
*.swp
*.swo
*~
.DS_Store

# Testing
coverage/

# Misc
.cache/
orca-legal-bullet-points_1.md
PRELAUNCH.md

# Chaos — local backups and regenerable fetched papers
backups/
chaos/backups/
chaos/papers/

# Chaos — regenerable reasoning outputs (per-paper cache + proposals)
chaos/reason_cache/
chaos/proposals.*
# Chaos — regenerable Gaia (Instantiator) cache (output of chaos/gaia.js)
chaos/gaia_cache/
# Chaos — regenerable Krius (Scout) cache (output of chaos/krius.js)
chaos/krius_cache/
# Chaos — regenerable run-identity sidecar (output of chaos/apply.js)
chaos/last-apply.json

# Chaos — regenerable read-only DB snapshot (output of chaos/snapshot.js).
# The script itself (snapshot.js) is tracked; its generated output is not.
# Narrow on purpose: must not swallow chaos/episodic/*.json (committed).
chaos/snapshot.json
