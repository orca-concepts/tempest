# Outreach mode — replaces the entire app with a landing page soliciting
# institutional partners. Set to "true" on Railway to enable; leave unset
# or "false" for normal operation. No code changes needed to toggle.
# VITE_OUTREACH_MODE=true
